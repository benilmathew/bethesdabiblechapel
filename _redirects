# Cloudflare Pages Redirects and Rewrites
# https://developers.cloudflare.com/pages/platform/redirects/

# Redirect old paths if any (add as needed)
# /old-path /new-path 301

# SPA-style routing - serve index.html for client-side routing if needed
# /* /index.html 200
